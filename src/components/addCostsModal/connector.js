export default (
    {
      addCostsModal: {
        isShowing,
        categories,
        isLoading,
        error,
        entity,
      },
    },
) => ({
  isShowing,
  categories,
  isLoading,
  error,
  entity,
});